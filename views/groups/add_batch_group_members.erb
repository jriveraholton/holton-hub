<div class="container">
    <div class="col-md-12">
        <a href="/manage/manage_groups" class="btn btn-outline-secondary" role="button"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-return-left" viewBox="0 0 16 16">
        <path fill-rule="evenodd" d="M14.5 1.5a.5.5 0 0 1 .5.5v4.8a2.5 2.5 0 0 1-2.5 2.5H2.707l3.347 3.346a.5.5 0 0 1-.708.708l-4.2-4.2a.5.5 0 0 1 0-.708l4-4a.5.5 0 1 1 .708.708L2.707 8.3H12.5A1.5 1.5 0 0 0 14 6.8V2a.5.5 0 0 1 .5-.5"/>
        </svg></a>
    </div>
    <div class="row">
        <h1>Add Group Members</h1>
    </div>
    <div class="row">
        <p>Please upload a text file of members for the selected club/sport</p>
        <p>Submit a CSV of only their emails</p>
    </div>
    <div class="row">
        <form action="/add_group_members" method="post" enctype="multipart/form-data">
            <div class="mb-3 col-md-4">
                <select id="group" name="group" class="form-select <% if @active_team_color == 'white'%>white-user<% else %>blue-user<% end %>" aria-label="Group select">
                <option selected disabled value>Select group to add members</option>
                <% @all_sports.each do |sport|%>
                    <option value="<%= sport.id %>"><%=sport.name.titleize%></option>
                <% end %>
                <hr>
                <% @all_clubs.each do |club|%>
                    <option value="<%= club.id %>"><%=club.name.titleize%></option>
                <% end %>
                </select>
            </div>
            <div class="mb-3">
                <label for="members_list" class="form-label">Upload members list</label>
                <input class="form-control <% if @active_team_color == 'white'%>white-user<% else %>blue-user<% end %>" type="file" id="members_list" name="members_list" required>
            </div>
            <div class="mb-3">
                <button type="submit" class="btn btn-primary mb-3 me-3">Submit</button>
            </div>
        </form>
    </div>
</div>
